#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: This is a longer description
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char s, l;

	for (s = 'a'; s <= 'z'; s++)
	{
	putchar(c);
	}
	for (l = 'A'; l <= 'Z'; l++)
	{
	putchar(l);
	}
	putchar('\n');
	return (0);
}
