#include "main.h"

/**
 * main - checks if positive_or_negative() prints correct output
 * when provided with 0
 *
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
